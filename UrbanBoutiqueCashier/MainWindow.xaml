<!--
 Author: Ochilov Ilyosjon (ID: B2300540)
 Project: Urban Boutique POS System - Front Office (Cashier)
-->
<Window x:Class="UrbanBoutiqueCashier.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Urban Boutique — Cashier POS"
        Height="720" Width="1100"
        MinHeight="600" MinWidth="950"
        Background="#f1f5f9"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Shared Button Style -->
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#6366f1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4f46e5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SuccessBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="#10b981"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#059669"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DangerBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="#ef4444"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#dc2626"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid row style with low-stock highlight -->
        <Style TargetType="DataGridRow">
            <Setter Property="Height" Value="36"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f8fafc"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="20,14" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#6366f1" CornerRadius="10" Width="40" Height="40" Margin="0,0,12,0">
                        <TextBlock Text="🏪" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="URBAN BOUTIQUE" FontSize="14" Foreground="#64748b" FontWeight="SemiBold"/>
                        <TextBlock Text="Cashier Terminal" FontSize="18" FontWeight="Bold" Foreground="#0f172a"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="👤 Sales Staff" FontSize="13" Foreground="#64748b" Margin="0,0,15,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Body -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="4*" MinWidth="360"/>
            </Grid.ColumnDefinitions>

            <!-- Products panel -->
            <Border Grid.Column="0" Background="White" CornerRadius="12" Margin="0,0,16,0" BorderBrush="#e2e8f0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="20,16" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1">
                        <TextBlock Text="📦 Available Products" FontSize="16" FontWeight="Bold" Foreground="#0f172a"/>
                    </Border>

                    <Grid Grid.Row="1" Margin="20,16,20,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtSearch" Grid.Column="0" ToolTip="Search by name or category..."/>
                        <Button Grid.Column="1" Content="🔍 Search" Width="110" Margin="10,0,0,0"
                                Style="{StaticResource PrimaryBtn}" Click="BtnSearch_Click"/>
                    </Grid>

                    <DataGrid x:Name="dgAvailableProducts" Grid.Row="2"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              SelectionMode="Single" GridLinesVisibility="None" HeadersVisibility="Column"
                              BorderThickness="0" Margin="0,0,0,0"
                              RowBackground="White" AlternatingRowBackground="#f8fafc"
                              ColumnHeaderHeight="38">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#f1f5f9"/>
                                <Setter Property="Foreground" Value="#64748b"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Padding" Value="16,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#e2e8f0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="16,8"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="PRODUCT" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="CAT." Binding="{Binding Category}" Width="100"/>
                            <DataGridTextColumn Header="SIZE" Binding="{Binding Size}" Width="60"/>
                            <DataGridTextColumn Header="COLOR" Binding="{Binding Color}" Width="80"/>
                            <DataGridTextColumn Header="PRICE" Binding="{Binding Price, StringFormat='$\{0:F2\}'}" Width="80"/>
                            <DataGridTextColumn Header="STOCK" Binding="{Binding StockQuantity}" Width="70"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="3" Padding="20,16" BorderBrush="#e2e8f0" BorderThickness="0,1,0,0" Background="#f8fafc">
                        <Button Content="+ ADD SELECTED TO CART" FontSize="14"
                                Style="{StaticResource SuccessBtn}" Click="BtnAddToCart_Click"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Cart panel -->
            <Border Grid.Column="1" Background="White" CornerRadius="12" BorderBrush="#e2e8f0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="20,16" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1">
                        <TextBlock Text="🛒 Current Order" FontSize="16" FontWeight="Bold" Foreground="#0f172a"/>
                    </Border>

                    <DataGrid x:Name="dgCart" Grid.Row="1"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              SelectionMode="Single" GridLinesVisibility="None" HeadersVisibility="Column"
                              BorderThickness="0" RowBackground="White" AlternatingRowBackground="#f8fafc"
                              ColumnHeaderHeight="36">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#f1f5f9"/>
                                <Setter Property="Foreground" Value="#64748b"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Padding" Value="14,8"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#e2e8f0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="14,8"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ITEM" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="QTY" Binding="{Binding Quantity}" Width="50"/>
                            <DataGridTextColumn Header="SUBTOTAL" Binding="{Binding Subtotal, StringFormat='$\{0:F2\}'}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" Padding="20" BorderBrush="#e2e8f0" BorderThickness="0,1,0,0" Background="#f8fafc">
                        <StackPanel>
                            <Button Content="− REMOVE SELECTED" Style="{StaticResource DangerBtn}" Click="BtnRemoveFromCart_Click" Margin="0,0,0,14"/>

                            <Border Padding="0,14,0,14" BorderBrush="#e2e8f0" BorderThickness="0,2,0,2">
                                <Grid>
                                    <TextBlock Text="TOTAL" FontSize="14" FontWeight="SemiBold" Foreground="#64748b" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="txtTotalAmount" Text="$0.00" FontSize="26" FontWeight="ExtraBold" Foreground="#10b981" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>

                            <Button Content="✔ COMPLETE CHECKOUT" FontSize="15" Padding="16,14" Margin="0,14,0,0"
                                    Style="{StaticResource SuccessBtn}" Click="BtnCheckout_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
