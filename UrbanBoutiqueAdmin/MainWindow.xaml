<!--
 Name: Ochilov Ilyosjon
 Student ID: B2300540
 Project: Urban Boutique POS System - Admin Panel
-->
<Window x:Class="UrbanBoutiqueAdmin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Urban Boutique — Admin"
        Height="720" Width="1080"
        MinHeight="600" MinWidth="950"
        Background="#f1f5f9"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="AccentBtn" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#6366f1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4f46e5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SuccessBtn" TargetType="Button" BasedOn="{StaticResource AccentBtn}">
            <Setter Property="Background" Value="#10b981"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#059669"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GhostBtn" TargetType="Button" BasedOn="{StaticResource AccentBtn}">
            <Setter Property="Background" Value="#f1f5f9"/>
            <Setter Property="Foreground" Value="#0f172a"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}"
                                BorderBrush="#e2e8f0" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#e2e8f0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <Border Grid.Row="0" Background="White" Padding="20,14" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#6366f1" CornerRadius="10" Width="40" Height="40" Margin="0,0,12,0">
                        <TextBlock Text="🛡" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="URBAN BOUTIQUE" FontSize="11" Foreground="#64748b" FontWeight="SemiBold"/>
                        <TextBlock Text="Admin Dashboard" FontSize="17" FontWeight="Bold" Foreground="#0f172a"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="👤 " FontSize="13" VerticalAlignment="Center"/>
                    <TextBlock x:Name="txtUserName" Text="Admin" FontSize="13" FontWeight="SemiBold" Foreground="#0f172a" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="Logout" Padding="14,8" Style="{StaticResource GhostBtn}" Click="BtnLogout_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Body -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Add product form -->
            <Border Grid.Column="0" Background="White" CornerRadius="12" BorderBrush="#e2e8f0" BorderThickness="1" Margin="0,0,16,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="24">
                        <TextBlock Text="➕ Add Product" FontSize="16" FontWeight="Bold" Foreground="#0f172a" Margin="0,0,0,16"/>

                        <TextBlock Text="PRODUCT NAME" FontSize="10" FontWeight="SemiBold" Foreground="#0f172a" Margin="0,0,0,6"/>
                        <TextBox x:Name="txtProductName" Margin="0,0,0,12"/>

                        <TextBlock Text="PRICE ($)" FontSize="10" FontWeight="SemiBold" Foreground="#0f172a" Margin="0,0,0,6"/>
                        <TextBox x:Name="txtPrice" Margin="0,0,0,12"/>

                        <TextBlock Text="CATEGORY" FontSize="10" FontWeight="SemiBold" Foreground="#0f172a" Margin="0,0,0,6"/>
                        <ComboBox x:Name="cmbCategory" Margin="0,0,0,12"/>

                        <TextBlock Text="SIZE" FontSize="10" FontWeight="SemiBold" Foreground="#0f172a" Margin="0,0,0,6"/>
                        <ComboBox x:Name="cmbSize" Margin="0,0,0,12">
                            <ComboBoxItem Content="S"/>
                            <ComboBoxItem Content="M" IsSelected="True"/>
                            <ComboBoxItem Content="L"/>
                            <ComboBoxItem Content="XL"/>
                            <ComboBoxItem Content="One Size"/>
                        </ComboBox>

                        <TextBlock Text="COLOR" FontSize="10" FontWeight="SemiBold" Foreground="#0f172a" Margin="0,0,0,6"/>
                        <TextBox x:Name="txtColor" Margin="0,0,0,12"/>

                        <TextBlock Text="STOCK QUANTITY" FontSize="10" FontWeight="SemiBold" Foreground="#0f172a" Margin="0,0,0,6"/>
                        <TextBox x:Name="txtStockQuantity" Margin="0,0,0,20"/>

                        <Button Content="💾 SAVE PRODUCT" Style="{StaticResource SuccessBtn}" Click="BtnSave_Click" Margin="0,0,0,10"/>
                        <Button Content="🔄 REFRESH" Style="{StaticResource GhostBtn}" Click="BtnRefresh_Click" Margin="0,0,0,20"/>

                        <Border BorderBrush="#e2e8f0" BorderThickness="0,1,0,0" Padding="0,16,0,0" Margin="0,4,0,0">
                            <StackPanel>
                                <Button Content="📊 REPORTS" Style="{StaticResource AccentBtn}" Click="BtnReports_Click" Margin="0,0,0,8"/>
                                <Button Content="👥 STAFF MANAGEMENT" Style="{StaticResource AccentBtn}" Click="BtnUserManagement_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right: Inventory grid -->
            <Border Grid.Column="1" Background="White" CornerRadius="12" BorderBrush="#e2e8f0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="24,18" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="📦 Inventory" FontSize="16" FontWeight="Bold" Foreground="#0f172a"/>
                                <TextBlock Text="Products with stock under 5 are highlighted" FontSize="11" Foreground="#64748b" Margin="0,2,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Background="#fef3c7" CornerRadius="6" Padding="8,4">
                                <TextBlock Text="⚠ Low stock" FontSize="11" FontWeight="SemiBold" Foreground="#92400e"/>
                            </Border>
                        </Grid>
                    </Border>

                    <DataGrid x:Name="dgProducts" Grid.Row="1"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              GridLinesVisibility="None" HeadersVisibility="Column"
                              BorderThickness="0" RowBackground="White" AlternatingRowBackground="#f8fafc"
                              ColumnHeaderHeight="38">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#f1f5f9"/>
                                <Setter Property="Foreground" Value="#64748b"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Padding" Value="16,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#e2e8f0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="16,8"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.CellStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="PRODUCT" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category}" Width="110"/>
                            <DataGridTextColumn Header="SIZE" Binding="{Binding Size}" Width="60"/>
                            <DataGridTextColumn Header="COLOR" Binding="{Binding Color}" Width="90"/>
                            <DataGridTextColumn Header="PRICE" Binding="{Binding Price, StringFormat='$\{0:F2\}'}" Width="85"/>
                            <DataGridTextColumn Header="STOCK" Binding="{Binding StockQuantity}" Width="70"/>
                        </DataGrid.Columns>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Height" Value="38"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                        <Setter Property="Background" Value="#fee2e2"/>
                                        <Setter Property="Foreground" Value="#991b1b"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="ToolTip" Value="⚠ Low stock — restock soon"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
